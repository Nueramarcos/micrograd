# Contributing to micrograd

1. Clone the repository: `git clone https://github.com/Nueramarcos/micrograd.git`
2. Run the smoke test: `python3 -c "import micrograd.engine as engine; Value = engine.Value; print(Value(1).data)"`
3. Note that micrograd is a minimal autograd teaching library.
